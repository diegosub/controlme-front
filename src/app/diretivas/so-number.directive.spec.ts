import { SoNumberDirective } from './so-number.directive';

describe('SoNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new SoNumberDirective(null);
    expect(directive).toBeTruthy();
  });
});
